from django.apps import AppConfig


class PrepDisplayConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.prep_display"
    verbose_name = "Prep Display (Kiosk)"
